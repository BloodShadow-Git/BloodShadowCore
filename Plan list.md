# TODO list

- Create test cases table (TestPlan.md)
- Write typst doc. Use new page for each namespace\class and need add mermaid\plantuml diagrams
- Write **Base64EncryptSystem** in **BloodShadow.Core.SaveSystem.StorageSystem.EncryptSystem**
- Write **BloodShadow.Core.ArchiveSystem** to process archives by **BloodShadow.Core.HybridStream** - use RAM if available, else drive
- Fix **BloodShadow.Core.CMD** and add to this

- Add to logger labels inherit abilities like `[Parent -> Child 1 -> Child 2]` and rewrite logger to use labels instead of direct calls
- Add custom channels to logger
